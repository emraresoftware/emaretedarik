version: '3.8'

services:
  app:
    build: .
    container_name: emaretedarik
    ports:
      - "8600:8600"
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
